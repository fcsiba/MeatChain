<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.android.codeaire.meatchain">

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.hardware.camera" />
    <uses-permission android:name="android.hardware.camera.autofocus" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".activities.authentication.ResetPasswordActivity"></activity>
        <activity android:name=".activities.MyProductDetailsActivity" />
        <activity android:name=".activities.productInfoScreens.UserDetail" />
        <activity android:name=".activities.Profile.UpdatePassword" />
        <activity android:name=".activities.Profile.UserProfileActivity" />
        <activity android:name=".activities.ViewProductsActivity" />
        <activity android:name=".activities.HelpSupportActivty" />
        <activity android:name=".activities.AboutUsActivty" />
        <activity android:name=".activities.Home.HomeScreen" />
        <activity android:name=".activities.rolesBasedScreens.PackagingForm" />
        <activity android:name=".activities.DisplayQRCodeActivity" />
        <activity android:name=".activities.productInfoScreens.ProductInfoActivity" />
        <activity android:name=".activities.rolesBasedScreens.DistributionForm" />
        <activity android:name=".activities.rolesBasedScreens.ButcherForm" />
        <activity android:name=".activities.productInfoScreens.FarmerInfo" />
        <activity android:name=".activities.rolesBasedScreens.FarmerForm" />
        <activity
            android:name=".activities.splash.SplashScreen"
            android:configChanges="orientation"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activities.scanner.ScannerActivity"
            android:configChanges="orientation"
            android:screenOrientation="portrait" />
        <activity
            android:name=".activities.authentication.SignUpActivity"
            android:configChanges="orientation"
            android:screenOrientation="portrait" />
        <activity
            android:name=".activities.authentication.LoginActivity"
            android:configChanges="orientation"
            android:screenOrientation="portrait" />
        <activity
            android:name=".MainActivity"
            android:configChanges="orientation"
            android:screenOrientation="portrait" />
    </application>

</manifest>